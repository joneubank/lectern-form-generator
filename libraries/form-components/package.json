{
  "name": "form-components",
  "version": "0.0.1",
  "description": "React Components to build a form dynamically ",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && NODE_PATH=./src tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "styled-components": "~5.3.5",
    "lectern": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^18.0.14",
    "typescript": "^4.7.4",
    "@types/styled-components": "~5.1.25"
  }
}
